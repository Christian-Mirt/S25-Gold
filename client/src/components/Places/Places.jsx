import './Places.css'
import { useNavigate } from 'react-router-dom';

function Places() {
  const navigate = useNavigate();

  return( 
    <div>
      <h1>Results for</h1>
    </div>
  )
}

export default Places;